# Работа в терминале
1. Установлен virtual box  
2. Установлен Vegrant  
3. windows Terminal  
4. С помощью Vegrant запущена виртуалка с  Ubuntu  
5. Виртуальной машине выделено 2 процессор, 1 Гб оперативной памяти, 64 Гб на жестком диске, одна сетевая карта за nat  
6. Для изменения количества ресурсов в файле необходимо прописать `` id ресурса = количество ``. Например  
```bash
config.vm.provider "virtualbox" do |v|
  v.memory = 1024
  v.cpus = 2
end
```
7. Выполнено  
8. `` HISTSIZE `` сторка 696. ignoreboth позволяет не сохранять в историю команды начинающиеся с пробела и дубликаты команд
9. {} используется для обозначения списка. Строка 220
10. `` touch {1..100000} ``.  Нет. Так как максимально возможное количество аргументов находится в переменной ``getconf ARG_MAX``. Начиная с Linux 2.6.23 ввели дополнительное ограничение, что один аргумент не должен быть больше чем  MAX_ARG_STRLEN (131072)  
11. Возвращает статус 0 или 1 в зависимости от оценки выражения внутри скобок. ``[[ -d /tmp ]]`` Проверяет наличие директории tmp.
12.   ```bash
           mkdir /tmp/new_path_directory
           ln -s /bin/bash /tmp/new_path_directory
           export PATH=/tmp/new_path_directory/:$PATH
      ```
''
13. at - запуск команд в определенное время. batch - запуск команд, когда нагрузка на систему будет ниже лимита  

14. `` vagrant halt ``
